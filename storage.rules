rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Materials — lecturers/admins upload, all signed-in users read
    match /materials/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && request.resource.size < 50 * 1024 * 1024; // 50 MB limit
    }

    // Assignment brief/instructions files — lecturers/admins upload, all signed-in users read
    match /assignments/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && request.resource.size < 50 * 1024 * 1024; // 50 MB limit
    }

    // Submissions — students upload their own only; lecturers/admins read
    match /submissions/{userId}/{assignmentId}/{fileName} {
      allow read:  if request.auth != null;
      allow write: if request.auth != null
        && request.auth.uid == userId
        && request.resource.size < 25 * 1024 * 1024; // 25 MB limit
    }

    // Forum attachments — any signed-in user can attach a file to their own
    // thread/reply; everyone signed in can read/download
    match /forum/{folder}/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && request.auth.uid == userId
        && request.resource.size < 25 * 1024 * 1024; // 25 MB limit
    }
  }
}
